import { StyleSheet } from 'react-native';
import { UiHeight, UiWidth, PtToDp } from 'method/Dimension';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});

export default styles;
